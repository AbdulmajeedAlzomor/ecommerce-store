# ecommerce-store

# E-commerce Store (PHP + MySQL)

## 📌 وصف المشروع
هذا المشروع عبارة عن متجر إلكتروني بسيط يتيح للمستخدم:
- تصفح المنتجات
- عرض تفاصيل كل منتج
- إضافة المنتجات إلى سلة التسوق
- إتمام عملية الشراء (Checkout)
- متابعة الطلبات

تم بناء المشروع باستخدام:
- PHP
- MySQL
- HTML / CSS / JavaScript

--## 🧩 المميزات الرئيسية
- صفحة رئيسية لعرض المنتجات المميزة
- صفحة عرض جميع المنتجات مع إمكانية تصفية/تصنيف (إن وجدت)
- صفحة تفاصيل المنتج (صورة، سعر، وصف...)
- سلة تسوق (إضافة/إزالة/تعديل الكمية)
- إتمام الطلب (Checkout)
- تسجيل الدخول / إنشاء حساب للمستخدم
- لوحة تحكم للمسؤول (Admin) لإدارة:
  - المنتجات (إضافة / تعديل / حذف)
  - الطلبات

---

## 🛠 المتطلبات
لتشغيل المشروع تحتاج:
- XAMPP أو WAMP أو Laragon (سيرفر محلي يدعم PHP + MySQL)
- متصفح (Chrome, Firefox, ...)

---

## ⚙️ طريقة التشغيل (Locally)

1. **نسخ ملفات المشروع**
   - انسخ مجلد المشروع `ecommerce-store` إلى:
     - مسار `htdocs` إذا كنت تستخدم XAMPP
     - أو مجلد `www` إذا كنت تستخدم WAMP (حسب البرنامج عندك)

2. **إنشاء قاعدة البيانات**
   - افتح `phpMyAdmin`
   - أنشئ قاعدة بيانات جديدة باسم مثلاً: `ecommerce`
   - من تبويب **Import** قم برفع ملف:
     - `database/ecommerce.sql`
   - اضغط **Go** لاستيراد الجداول

3. **إعداد الاتصال بقاعدة البيانات**
   - افتح الملف: `config/db_config.php`
   - تأكد من ضبط القيم على حسب إعدادات السيرفر المحلي:
     ```php
     $host = "localhost";
     $user = "root";
     $password = "";
     $dbname = "ecommerce";
     ```

4. **تشغيل المشروع**
   - افتح المتصفح واكتب في شريط العنوان:
     - `http://localhost/ecommerce-store/`
   - ستظهر لك الصفحة الرئيسية للمتجر.

---

## 🔐 بيانات الدخو )


- حساب مسؤول (Admin):
  - البريد: `admin@example.com`
  - كلمة المرور: 'admin'
  - 123`


## 🧱 هيكلة المشروع (باختصار)

```text
ecommerce-store/
├── index.php              # الصفحة الرئيسية
├── products.php           # عرض المنتجات
├── product_details.php    # تفاصيل المنتج
├── cart.php               # سلة التسوق
├── checkout.php           # إتمام الشراء
├── orders.php             # طلبات المستخدم
├── login.php              # تسجيل الدخول
├── register.php           # إنشاء حساب
├── admin/                 # لوحة التحكم
├── config/db_config.php   # إعدادات قاعدة البيانات
├── assets/                # ملفات CSS/JS/Images
└── database/ecommerce.sql # ملف قاعدة البيانات
